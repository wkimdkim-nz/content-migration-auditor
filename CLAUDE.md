## Agent skills

### Issue tracker

Issues live in GitHub Issues for `wkimdkim-nz/content-migration-auditor` (via `gh`). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
